{
  "name": "math_maker",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "git": "git add . && git commit -am 'auto-commit' && git push origin master"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.7.9",
    "gulp": "^3.9.1"
  },
  "dependencies": {
    "express": "^4.14.0"
  }
}
